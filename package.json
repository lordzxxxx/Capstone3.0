{
  "scripts": {
    "test:firestore-rules": "firebase emulators:exec --only firestore --project demo-capstone-security \"mocha --timeout 60000 tests/firestore.rules.test.cjs\""
  },
  "dependencies": {
    "firebase": "^12.8.0"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase-tools": "^15.24.0",
    "mocha": "^11.0.0"
  }
}
