{
  "rules": {
    ".read": false,
    ".write": false,
    "users": {
      "$uid": {
        ".read": "auth != null && (auth.uid === $uid || auth.token.role === 'ADMIN' || auth.token.role === 'SUPER_ADMIN' || auth.token.role === 'CHO_SUPER_ADMIN')",
        ".write": "auth != null && ((auth.token.role === 'ADMIN' || auth.token.role === 'SUPER_ADMIN' || auth.token.role === 'CHO_SUPER_ADMIN') || (auth.uid === $uid && ((!data.exists() && newData.child('uid').val() === auth.uid && newData.child('email').val() === auth.token.email && (newData.child('role').val() === 'CHO' || newData.child('role').val() === 'BHW') && newData.child('approvalStatus').val() === 'approved' && newData.child('accountStatus').val() === 'active') || (data.exists() && newData.child('uid').val() === data.child('uid').val() && newData.child('email').val() === data.child('email').val() && newData.child('role').val() === data.child('role').val() && newData.child('accessScope').val() === data.child('accessScope').val() && newData.child('approvalStatus').val() === data.child('approvalStatus').val() && newData.child('accountStatus').val() === data.child('accountStatus').val() && newData.child('barangay').val() === data.child('barangay').val() && newData.child('barangayCode').val() === data.child('barangayCode').val() && newData.child('barangayDistrict').val() === data.child('barangayDistrict').val())))",
        ".validate": "newData.hasChildren(['uid', 'email', 'role', 'approvalStatus', 'accountStatus'])"
      }
    }
  }
}
